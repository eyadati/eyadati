description = "adding features"
prompt = """
Execute these tasks:
-check user favorite clinic functions since user favorites page has clinics with no data so either the data isn't called correctly or the data saved isn't correct
-theme resets to light mode after closing the app and opening it again and theme switch doesn't work in clinic side
-logo is too small
-managment slots are not correct since they contain break times and it should also only givr present and future slots not past ones too
-bottom nav bar is overlaying some widgets so make sure pages included in the nav bar is properly displayed
"""