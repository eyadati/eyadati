import 'package:flutter/material.dart';

class ThemeConstants {
  // Brand Colors (Algerian-friendly/Professional)
  static const Color primaryBlue = Color(0xFF1565C0);   // Blue 800: Trust
  static const Color secondaryGreen = Color(0xFF388E3C); // Green 700: Health
  static const Color backgroundLight = Color(0xFFFAFAFA); // Gray 50: Clean
  static const Color surfaceWhite = Colors.white;
  static const Color textMain = Color(0xFF212121);      // High Contrast
  static const Color textSecondary = Color(0xFF757575); // Accessibility
  static const Color errorRed = Color(0xFFD32F2F);      // Red 700

  // Spacing
  static const double borderRadius = 12.0;
  static const EdgeInsets defaultPadding = EdgeInsets.symmetric(horizontal: 16, vertical: 8);
}
